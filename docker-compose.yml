services:
  postpilot-server:
    build: .
    container_name: postpilot-server
    restart: always
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml
    network_mode: host
